const nomePrimeiro = prompt("escreva aqui: ");
const nomeSegundo = prompt("qual é seu segundo nome?: ");
const nomeCompleto = nomePrimeiro + " " + nomeSegundo;
console.log(`Então seu nome é ${nomeCompleto}`);